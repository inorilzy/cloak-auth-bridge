[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "cloak-browser-auth"
version = "0.4.1"
description = "Local Cloak browser auth MCP with Chrome login sync and reverse tools"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "cloakbrowser>=0.5.6,<0.6",
  "mcp>=1.0,<2",
  "playwright>=1.62,<1.63",
  "pydantic>=2.10,<3",
  "pywin32>=308; sys_platform == 'win32'",
  "websockets>=14,<17"
]

[project.optional-dependencies]
dev = [
  "mypy>=1.15,<2",
  "pytest>=8,<9",
  "pytest-asyncio>=0.24,<2",
  "ruff>=0.9,<1"
]

[project.scripts]
cloak-browser-auth = "cloak_browser_auth.main:main"
cloak-browser-auth-mcp = "cloak_browser_auth.main:main"

[project.urls]
Homepage = "https://github.com/inorilzy/cloak-browser-auth"
Repository = "https://github.com/inorilzy/cloak-browser-auth"

[tool.setuptools.packages.find]
include = ["cloak_browser_auth*"]

[tool.setuptools.package-data]
cloak_browser_auth = [
  "defaults/profiles.json",
  "defaults/sites/*.json",
  "defaults/extension/*",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.mypy]
python_version = "3.12"
check_untyped_defs = true
disallow_untyped_defs = true
warn_unused_ignores = true
exclude = [".venv"]

[tool.ruff]
line-length = 110
target-version = "py312"


[tool.ruff.lint.per-file-ignores]
# CDP attach paths intentionally swallow non-fatal protocol errors.
"cloak_browser_auth/reverse_session.py" = ["S110", "S112", "BLE001"]
"cloak_browser_auth/mcp_server.py" = ["S110", "BLE001"]
"cloak_browser_auth/debug_session.py" = ["S110", "BLE001"]
